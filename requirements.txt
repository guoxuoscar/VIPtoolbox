# 安装命令（在文件所在目录执行）：
# python -m pip install -r requirements.txt && python -m playwright install chromium
# 
# 如果 rembg 装完后不能用，再跑一次：
# python -m pip install --force-reinstall llvmlite packaging colorama attrs
PySide6>=6.5.0
numpy>=1.24.0,<3
pandas>=1.5.0
Pillow>=10.0.0
PyMuPDF>=1.21.0
openpyxl>=3.1.0
xlwt>=1.3.0
playwright>=1.40.0
rembg>=2.0.0
onnxruntime>=1.16.0
pywin32>=305
